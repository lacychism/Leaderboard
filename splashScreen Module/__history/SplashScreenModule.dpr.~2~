program SplashScreenModule;

uses
  System.StartUpCopy,
  FMX.Forms,
  joinLeagueUnit in 'joinLeagueUnit.pas',
  net in 'net.pas',
  ObtainDatabase in 'ObtainDatabase.pas' {Form1},
  selectpick in 'selectpick.pas',
  Unit2 in 'Unit2.pas' {Form2},
  xmlLoad in 'xmlLoad.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm2, Form2);
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
